<UserControl x:Class="GenShin_Launcher_Plus.Views.UsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Border Style="{StaticResource CardBorder}" Width="380" Padding="24"
            VerticalAlignment="Center" HorizontalAlignment="Center">
        <StackPanel>
            <TextBlock Text="{Binding languages.AddUsersPageTitle}"
                       Style="{StaticResource SubtitleText}" Margin="0,0,0,4"/>
            <TextBlock Text="{Binding languages.AddUsersPageSubTitle}"
                       Style="{StaticResource CaptionText}" Margin="0,0,0,20"/>
            <TextBox Style="{StaticResource InputTextBox}"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,16"/>
            <CheckBox Content="{Binding languages.SaveGameConfigCkb}" Style="{StaticResource ToggleSwitch}"
                      IsChecked="{Binding IsSaveGameConfig}" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{Binding languages.Cancel}" Style="{StaticResource SecondaryButton}"
                        Command="{Binding RemoveThisPageCommand}" Margin="0,0,8,0"/>
                <Button Content="{Binding languages.SaveBtn}" Style="{StaticResource PrimaryButton}"
                        Command="{Binding SaveUserDataCommand}"/>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
