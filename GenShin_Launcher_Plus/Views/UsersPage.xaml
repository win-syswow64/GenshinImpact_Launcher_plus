<UserControl x:Class="GenShin_Launcher_Plus.Views.UsersPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="36">
        <Border Style="{StaticResource SubPageSurface}" Width="460" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,24">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                    <Border Width="44" Height="44" Background="#2AFFFFFF" CornerRadius="12">
                        <TextBlock Text="&#xE77B;" FontFamily="{StaticResource IconFont}" FontSize="20" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding languages.AddUsersPageTitle}" Style="{StaticResource SubtitleText}"/>
                        <TextBlock Text="{Binding languages.AddUsersPageSubTitle}" Style="{StaticResource CaptionText}" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>
                <StackPanel Grid.Row="1">
                    <TextBlock Text="账号备注" Foreground="#CFFFFFFF" FontSize="12" Margin="0,0,0,7"/>
                    <TextBox Style="{StaticResource InputTextBox}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
                <Border Grid.Row="2" Background="#241A2430" BorderBrush="#2BFFFFFF" BorderThickness="1" CornerRadius="10" Padding="12" Margin="0,18,0,22">
                    <CheckBox Content="{Binding languages.SaveGameConfigCkb}" Style="{StaticResource ToggleSwitch}" IsChecked="{Binding IsSaveGameConfig}"/>
                </Border>
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{Binding languages.Cancel}" Style="{StaticResource SecondaryButton}" Command="{Binding RemoveThisPageCommand}" Margin="0,0,8,0"/>
                    <Button Content="{Binding languages.SaveBtn}" Style="{StaticResource PrimaryButton}" Command="{Binding SaveUserDataCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
