<UserControl x:Class="GenShin_Launcher_Plus.Views.GuidePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" Background="#202020">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" Text="{Binding languages.WelcomeTitle}"
                   Style="{StaticResource TitleText}" HorizontalAlignment="Center"
                   Margin="0,0,0,8"/>
        <TextBlock Grid.Row="2" Text="{Binding languages.BootstrapTitle}"
                   Style="{StaticResource CaptionText}" HorizontalAlignment="Center"
                   Foreground="#80FFFFFF" Margin="0,0,0,32"/>

        <Border Grid.Row="3" Style="{StaticResource CardBorder}" MaxWidth="500" Padding="24">
            <StackPanel>
                <TextBlock Text="{Binding languages.FinalTipLabel}" Foreground="#B0FFFFFF"
                           FontSize="14" Margin="0,0,0,16"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Style="{StaticResource InputTextBox}"
                             Text="{Binding GamePath, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Grid.Column="1" Content="..." Style="{StaticResource SecondaryButton}"
                            Command="{Binding DirchooseCommand}" Width="40" Margin="8,0,0,0"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
